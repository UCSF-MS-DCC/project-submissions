<%= form_for(@myo_file) do |f| %>
  <% if @myo_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myo_file.errors.count, "error") %> prohibited this myo_file from being saved:</h2>

      <ul>
      <% @myo_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trac_visit_id %><br>
    <%= f.number_field :trac_visit_id %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
